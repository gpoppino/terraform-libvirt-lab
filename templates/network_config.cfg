version: 2
ethernets:
  eth0:
    %{ if dhcp == "true" }dhcp4: true
    %{ else }dhcp4: false
    addresses:
      - ${ip_address}/${netmask}
    gateway4: ${gateway}
    nameservers:
      addresses: ${nameservers}
    %{ endif }
